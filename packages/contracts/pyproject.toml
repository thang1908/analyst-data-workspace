[build-system]
requires = ["flit_core >=3.2,<4"]
build-backend = "flit_core.buildapi"

[project]
name = "cx-contracts"
version = "0.1.0"
description = "Contracts and schemas for CX Platform"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pydantic>=2.6.0"
]

[project.optional-dependencies]
test = [
    "pytest>=8.0.0",
    "mypy>=1.8.0"
]
